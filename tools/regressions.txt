# FLOW regression markers
#
# One fix per line:
#     <file> | <literal substring that must be present> | <what it protects against>
#
# Blank lines and lines starting with # are ignored. The pattern is a LITERAL
# substring, not a regex - keep it short, distinctive, and unlikely to be
# reworded during ordinary editing. Prefer a constant or function name over a
# sentence.
#
# WHY THIS FILE EXISTS
# Every entry below was a real, diagnosed bug that was fixed and then LOST to a
# later merge, or is a fix whose loss would be silent. Two were lost twice.
# None of them announce themselves when they go: the mod loads, the log is
# clean, and the symptom comes back looking new. Checking for the marker is the
# only cheap way to notice.
#
# Add a line whenever a fix would be silent if reverted.

core/sensor.lua          | rayGet                         | SharedRay: another mod's copy may lack getUnclipped; resolve the accessor, not the interface
core/sensor.lua          | type(dist)                     | SharedRay: winning copy may omit ray.distance - derive it from hitPos instead
states/airborne.lua      | isWorldPaused()                | Roll never armed: isWorldPaused is a FUNCTION; without parens the handler always returned
states/airborne.lua      | ROLL_HEIGHT_WINDOW             | Roll arm-early exploit: a press at apex armed the whole descent
main.lua                 | OVERRIDE_STATES                | Jump key going dead: a state left without exit() keeps overrideMovementControls set
core/state_manager.lua   | activeState.abort              | Refused Vault/Mantle announced success and started an animation anyway
states/mantle.lua        | vouchDestination               | LedgeHang climb refused by Mantle re-validating a ledge already proven by the grab
states/mantle.lua        | BLOCK_RETRY_RADIUS             | Mantle had no refusal suppression; thrash stole every other airborne frame
states/vault.lua         | BLOCK_RETRY_RADIUS             | Blanket 0.6s block made Vault unresponsive while approaching geometry
states/ledge_hang.lua    | LIP_SANITY_RANGE               | Teleport to 0,0: a stale cross-cell lip handed over by Shimmy was consumed later
global/flow_amf_backend.lua | MAX_SNAP_DISTANCE           | Backstop: refuse a nil or absurd snap position rather than teleporting to the cell origin
states/shimmy.lua        | sideMovement                   | Shimmy transport: per-tick teleport shook the camera, one-snap-per-step teleported point to point
playerAnim.lua           | isPlaying(self, currentGroup)  | AnimRefresh v3 confirm pass restarted short one-shots mid-play
playerAnim.lua           | lastVariant                    | resolveGroup fell back to "right"; Ladder's up/down/idle resolved to nil
FLOW_AMF.omwscripts      | AnimRefresh_v3                 | v3 fixes a lost-refresh hole in v1/v2; the filename carries the version deliberately
